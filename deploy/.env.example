# ongrid — local dev compose (deploy/docker-compose.yml)
#
# Copy to deploy/.env and adjust. Every value below has a sane default in the
# compose file, so the stack also boots with no .env at all — this file just
# covers the handful you'll usually want to set for local development.
#
# This is a LOCAL DEV convenience stack: no TLS, trivial DB password baked into
# the compose file. Do NOT expose it beyond localhost. For production use the
# release package under deploy/install/ instead.

# --- First admin (seeded on first boot; if empty, no admin is created) ---
ONGRID_ADMIN_EMAIL=admin@ongrid.local
ONGRID_ADMIN_PASSWORD=change-me

# --- JWT signing secret (override the insecure default) ---
ONGRID_JWT_SECRET=change-me-to-a-long-random-string

# --- LLM endpoint (any OpenAI-compatible API; empty key => AI chat is disabled) ---
ONGRID_OPENAI_API_KEY=
ONGRID_OPENAI_MODEL=gpt-4o
# ONGRID_OPENAI_BASE_URL=https://api.openai.com/v1

# --- Kubernetes edge enrollment ---
# Public URL and tunnel address must be reachable from the target Kubernetes cluster.
ONGRID_PUBLIC_URL=
ONGRID_TUNNEL_ADDR=
# Kubernetes Event snapshot guards. Events are kept as the current troubleshooting
# cache, not an unbounded history table.
ONGRID_K8S_EVENT_RETENTION=24h
ONGRID_K8S_EVENT_MAX_PER_CLUSTER=5000
ONGRID_K8S_EVENT_CLEANUP_INTERVAL=1h

# --- Embedded Grafana (local dev defaults; production install generates random) ---
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
# ONGRID_GRAFANA_INTERNAL_URL=http://grafana:3000/grafana
# Leave empty to disable embedded Grafana token bootstrap.
# ONGRID_GRAFANA_BOOTSTRAP_PASSWORD=

# --- Embedding provider for knowledge base / RAG ---
ONGRID_EMBEDDING_PROVIDER=local
ONGRID_EMBEDDING_MODEL=bge-small-zh-v1.5
ONGRID_EMBEDDING_DIM=512
ONGRID_EMBEDDING_API_KEY=
ONGRID_EMBEDDING_BASE_URL=

# --- LLM Wiki persistent workspace ---
ONGRID_LLM_WIKI_ENABLED=true
ONGRID_LLM_WIKI_DIR=/var/lib/ongrid/llm-wiki
ONGRID_LLM_WIKI_TIMEOUT_SECONDS=600
ONGRID_LLM_WIKI_LEAF_BATCH_INPUT_TOKENS=32000
ONGRID_LLM_WIKI_LEAF_BATCH_OUTPUT_TOKENS=8000
ONGRID_LLM_WIKI_LEAF_BATCH_SAFETY_TOKENS=4000
ONGRID_LLM_WIKI_ENABLE_LEAF_BATCH=true
ONGRID_LLM_WIKI_ENABLE_CHUNK_CACHE=true
ONGRID_LLM_WIKI_ENABLE_SOURCE_SYNTHESIS=true
ONGRID_LLM_WIKI_LEAF_MODEL_VERSION=

# --- Embedded observability retention (test environments can lower these) ---
ONGRID_PROM_RETENTION_TIME=2160h
ONGRID_PROM_RETENTION_SIZE=20GB
ONGRID_LOKI_RETENTION_PERIOD=720h
ONGRID_TEMPO_BLOCK_RETENTION=168h

# Test-only direct apply. Enable only when Manager runs on the Compose host.
# ONGRID_OBSERVABILITY_COMPOSE_DIR=/absolute/path/to/ongrid
# ONGRID_OBSERVABILITY_ENV_FILE=/absolute/path/to/ongrid/deploy/.env
# ONGRID_OBSERVABILITY_COMPOSE_FILES=deploy/docker-compose.yml
# ONGRID_OBSERVABILITY_COMPOSE_PROJECT=ongrid
